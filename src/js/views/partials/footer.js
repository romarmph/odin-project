import { createPairedElement } from "../../components/factory";
import { Div } from "../../components/layout";
import { P, Span, Img } from "../../components/content";

const footer = createPairedElement({
	element: 'footer',
	child: Div({

	})
})
